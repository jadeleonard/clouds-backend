generator client {
  provider = "prisma-client-js"
}








datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
  relationMode = "prisma"
}

model faq {
  id          String @id @default(auto()) @map("_id") @db.ObjectId
  description String
  title       String
}

model hero {
  id          String @id @default(auto()) @map("_id") @db.ObjectId
  description String
  title       String
}

model navbar {
  id   String @id @default(auto()) @map("_id") @db.ObjectId
  href String
  name String
}

model items {
  id          String @id @default(auto()) @map("_id") @db.ObjectId
  name        String
  image       String
  description String
  price       Int
}

model user {
  id       String @id @default(auto()) @map("_id") @db.ObjectId
  address  String
  email    String
  lastname String
  name     String
  password String
}
